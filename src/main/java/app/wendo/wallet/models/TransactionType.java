package app.wendo.wallet.models;

public enum TransactionType {
    TRIP_EARNING,           // Driver earnings from a trip
    PLATFORM_COMMISSION,    // Commission owed to the platform
    PAYMENT_TO_PLATFORM     // Payment made by driver to the platform
}